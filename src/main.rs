// Throw some const generics in here so we break on anything less than 1.51.
// #[allow(dead_code)]
// struct RectangularArray<T, const WIDTH: usize, const HEIGHT: usize> {
//     array: [[T; WIDTH]; HEIGHT],
// }

fn main() {
    println!("Hi, world!");
}
